# Copy to .env and fill in. .env is gitignored.
#
# AnimeOnsen search bearer token (value only, without "Bearer " prefix).
# Used for the Meilisearch catalog lookup. The video bearer is derived
# dynamically server-side (no static value needed).
VITE_ANIMEONSEN_SEARCH_TOKEN=
